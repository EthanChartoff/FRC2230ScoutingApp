import 'package:flutter/material.dart';

/// TODO: build this page
class DashboardNoDataPage extends StatelessWidget {
  const DashboardNoDataPage({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('No data to show'),
    );
  }
}